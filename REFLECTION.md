# REFLECTION.md — AI4SE 期末项目反思报告

> 学生：唐海悦
> 学号：241880266
> 项目：CampusHub AI — 智能校园互助服务平台
> 日期：2026-08-12

---

## 一、引言

本项目将原有的 CampusHub 校园互助平台改造为 AI4SE 课程项目，核心工作是：在现有 CRUD 系统上集成 AI 需求分析功能和校园助手对话机器人，并完成凭据安全存储、Docker 分发、CI/CD 等工程化要求。整个过程使用 Superpowers 方法论指导，从 SPEC 编写到 subagent 任务派发，再到人工 review 和修正。

本文是对整个过程的反思：Superpowers 方法论哪些有效、哪些流于形式；TDD 在 AI 协作中是阻碍还是助力；subagent 工作流能走多远；以及我对当前 agentic SE 方法论的批判性思考。

---

## 二、Superpowers 技能评估

### 2.1 发挥最大作用的技能

**spec（规约编写）** 是整个过程最有价值的技能。在没有任何 AI 功能的情况下，通过 brainstorming 将模糊的"加 AI"转化为具体的"AI 需求分析 + 校园助手"两个明确功能，并确定了技术边界（不侵入核心业务、支持多 LLM 提供商）。这个过程如果只靠自己和 AI 随意对话，大概率会陷入"什么都想加"或"不知道加什么"的困境。spec 的结构化模板（概述、技术选型、安全设计、API 设计、测试策略）强制我思考了每个维度的细节。

**plan（计划拆分）** 也发挥了关键作用。将整个项目拆分为 8 个 subagent task，每个 task 有明确的输入（prompt）、输出（文件列表）和完成标准，使得 subagent 派发时有据可依。这类似于软件工程中的 WBS（工作分解结构），但 Superpowers 的 plan 更注重"什么是 AI 能独立完成的最小单元"。

### 2.2 "形式大于实质"的技能

**review（代码审查）** 在使用中发现形式大于实质。Superpowers 的 review 技能主要关注代码风格、命名规范等表层问题，但对于 AI 生成代码的真正风险——逻辑错误、边界条件遗漏、安全漏洞——缺乏深度检查能力。最终我不得不自己逐行审查 AI 生成的代码，特别是 `credentialManager.js` 中的加密逻辑和 `aiService.js` 中的 LLM 调用错误处理。这说明当前的 AI review 能力还远不能替代有经验的人工审查。

**tdd（测试驱动）** 在部分场景中显得形式化。对于 AI 功能模块，mock LLM 写测试确实有价值（快速、稳定、零成本），但对于已有的 CRUD 模块，在已有完整测试覆盖的情况下，强制"先写测试再改代码"只是增加了不必要的步骤。

---

## 三、TDD 在 AI 协作中的角色

### 3.1 TDD 作为放大器

在 AI 协作场景下，TDD 的价值被放大了。当 subagent 生成代码后，测试是验证"它是否做了我想要的"的最快方式。特别是 `ai.test.js`，通过 mock credentialManager 和 fetch，我在 2 秒内就能确认 AI 模块的核心逻辑是否正确，而不需要真实调用 LLM API（耗时 + 花钱）。

测试也帮助我发现了 subagent 生成的代码中的一个 bug：`aiService.js` 中的 `analyzeRequirement` 函数在 LLM 返回非 JSON 格式时没有正确的 fallback 处理。这是因为测试用例覆盖了边界情况，而 subagent 生成的代码只考虑了正常路径。

### 3.2 TDD 的局限性

TDD 在 AI 协作中的局限性也很明显：**测试的质量取决于 prompt 的质量**。如果我在 prompt 中没有明确要求"处理 LLM 返回非 JSON 格式的情况"，测试用例也不会覆盖这个场景。TDD 不能替代对问题的深入思考——它只是验证"代码做了我让它做的事"，但无法验证"我让它做的事是否正确"。

另一个局限是：TDD 在探索性开发阶段效率低下。当我不确定 AI 模块的接口应该如何设计时，先写测试会限制探索空间。我在 `aiService.js` 的开发中就遇到了这个问题——最初设计的接口（`analyzeRequirement` 直接返回 JSON）在后续开发中发现不够灵活，需要重构。如果一开始就写死了测试，重构成本会更高。

---

## 四、Subagent 工作流的自主性边界

### 4.1 Subagent 能自主运行多远？

在我的项目中，subagent 能自主完成一个 task 的时间大约是 2-5 分钟，生成的代码量在 50-200 行。超过这个范围，subagent 的输出质量会明显下降：

- **代码变长后逻辑一致性下降**：当 `aiService.js` 超过 150 行时，subagent 开始出现前后不一致的问题（如函数签名变化、变量命名冲突）
- **缺少上下文感知**：subagent 不知道 `credentialManager.js` 已经被修改过，可能生成与最新代码不兼容的调用方式
- **无法自我纠错**：subagent 不会意识到自己的输出有 bug，需要人工审查发现

### 4.2 最优的 Task 颗粒度

经过实践，我发现最优的 task 颗粒度是：**一个 task = 一个文件 + 一个明确的功能**。例如：

- "创建 credentialManager.js，实现加密存储" ✅ 合适
- "实现整个 AI 模块（包括加密、调用、路由、测试）" ❌ 太粗
- "在 credentialManager.js 的 encrypt 函数中添加 IV 生成" ❌ 太细

这个粒度与"一个函数"或"一个类"的软件工程模块化粒度基本一致，这并非巧合——好的软件设计自然会产生适合 AI 协作的任务粒度。

### 4.3 人工干预的必要性

即使在最顺利的 subagent 任务中，人工干预也必不可少：

1. **安全审查**：`credentialManager.js` 中的加密逻辑必须人工确认
2. **业务逻辑验证**：`aiService.js` 中的 system prompt 直接影响 AI 输出质量，需要人工调优
3. **集成测试**：subagent 只能做单元测试，集成测试需要人工设计
4. **文档润色**：AI 生成的文档有"模板感"，需要注入个人经验和批判性思考

---

## 五、SPEC / PLAN 质量对实现质量的影响

### 5.1 一个"规约不清导致偏离"的具体案例

在编写 `aiService.js` 的初始 prompt 时，我写了：

> "实现一个 AI 服务，调用 LLM 分析需求"

这个 prompt 过于模糊，导致 subagent 生成了一个返回自由文本的版本，而不是结构化的 JSON。这导致前端无法解析结果，需要重新生成。

修正后的 prompt：

> "实现 `analyzeRequirement(title, description)` 函数，调用 LLM 返回 JSON 格式的 `{category, estimatedPrice, tags, riskLevel, suggestion}`。使用 system prompt 约束 LLM 输出格式。处理 LLM 返回非 JSON 的容错情况。"

这次 subagent 生成的代码质量明显更好，几乎不需要人工修改。

**教训**：SPEC 中的函数签名、输入输出格式、边界条件处理，越具体越好。模糊的 SPEC 会导致 subagent 自由发挥，结果往往偏离预期。

### 5.2 SPEC 质量与实现质量的正相关

统计我的 8 个 subagent task：

| SPEC 详细程度 | Task 数量 | 平均人工修改行数 |
|--|--|--|
| 高（明确函数签名、输入输出、边界条件） | 4 | 5 行 |
| 中（描述功能但不涉及细节） | 3 | 20 行 |
| 低（一句话描述） | 1 | 50+ 行（几乎重写） |

数据清晰地表明：SPEC 越详细，subagent 输出质量越高，人工修改越少。

---

## 六、Prompt / Context 策略

### 6.1 最有效的策略

**策略 1：角色设定 + 输出格式约束**

在 `aiService.js` 的 system prompt 中，我明确设定了 AI 的角色（"你是 CampusHub 的 AI 助手"）和输出格式（"以 JSON 格式返回"）。这显著提高了 LLM 输出的可用性。

**策略 2：example-driven prompting**

在 `analyzeRequirement` 的 prompt 中，我给出了分类规则的具体示例（"配送/取件/代拿 → express"），这让 LLM 的分类准确率从约 70% 提升到接近 95%。

**策略 3：错误处理分层**

我将错误处理分为三层：LLM API 层（网络错误、认证失败）、格式解析层（JSON 解析失败）、业务层（无效分类）。每层有独立的错误处理和 fallback。这让系统在 LLM 不稳定时仍能优雅降级。

### 6.2 为什么有效

这些策略的共同点是：**减少 LLM 的不确定性空间**。LLM 本质上是一个概率系统，给它越多的约束（角色、格式、示例、边界），输出就越可预测。这与"给 AI 更多自由让它发挥创造力"的直觉相反——在工程场景中，约束是质量的保证。

---

## 七、凭据与分发：被忽略的工程问题

### 7.1 凭据安全

在 CampusHub 原始项目中，根本没有考虑凭据问题（因为没有调用 LLM API）。添加 AI 功能后，凭据安全立刻成为必须面对的工程问题：

- **"API Key 放哪？"** 最简单的做法是放 `.env`，但明文存储的风险让我重新思考
- **"加密密钥从哪来？"** 用主密码派生，但主密码的管理又成了新问题
- **"容器重启后怎么办？"** 需要持久化卷或重新解锁

这些问题在设计 CRUD 应用时完全不会遇到，但一旦涉及外部 API 调用，它们就是不可回避的工程现实。

### 7.2 分发

Docker 分发看似简单（`docker build && docker run`），但实际遇到的问题包括：

- 前端 SPA 路由需要 Nginx 配置（`try_files`）
- API 代理配置（前端 → 后端）
- 凭据卷的持久化
- 健康检查配置

这些问题如果不做分发，永远不会被发现。"在一台全新机器上从零运行"这个要求，暴露了很多开发环境中的隐式假设。

---

## 八、如果重做我会改变什么

1. **更早地确定 SPEC 细节**：在写第一行代码之前，花更多时间在 SPEC 上，特别是 API 接口的输入输出格式
2. **使用 TypeScript**：JS 的灵活性在 subagent 协作中反而成了负担——类型检查可以捕获很多 AI 生成的类型错误
3. **先做集成测试再做单元测试**：单元测试 mock 了太多东西，导致集成时才发现问题
4. **减少 subagent 任务数量**：8 个 task 太多了，合并为 4-5 个更合理
5. **加入 E2E 测试**：至少一个端到端的测试用例，验证整个流程

---

## 九、对 Superpowers 方法论的批判

### 9.1 Superpowers 的假设

Superpowers 假设：
1. **开发者清楚地知道要做什么**：但实际中，需求往往在开发过程中才逐渐清晰
2. **subagent 可以独立完成一个 task**：但实际上，subagent 之间需要大量的上下文共享
3. **TDD 总是有益的**：但在探索性开发中，TDD 会限制灵活性
4. **review 可以发现 AI 代码的问题**：但当前的 review 能力还远不够

### 9.2 这些假设在我的项目中成立吗？

假设 1 **部分成立**：对于凭据管理和 Docker 分发这种工程化需求，假设成立；但对于 AI 功能的设计，我在开发过程中多次调整了方向。

假设 2 **不成立**：我的 subagent 任务之间有明显的依赖关系（如 ai-routes 依赖 ai-service），但 subagent 之间没有共享上下文，导致接口不匹配。

假设 3 **部分成立**：对于稳定接口的模块，TDD 很有价值；对于探索性模块，TDD 增加了不必要的开销。

假设 4 **不成立**：当前的 AI review 主要关注代码风格，无法发现逻辑错误和安全漏洞。

### 9.3 我的核心观点

Superpowers 的最大价值不在于它的具体技能（spec、plan、tdd、review），而在于它**强制执行的工程纪律**。在 AI 能快速生成代码的时代，很容易陷入"生成 → 复制 → 粘贴 → 运行"的快速循环，跳过了思考和验证。Superpowers 的流程（先 spec 后 plan 再 code）强制你在动手之前思考，在提交之前验证。

但 Superpowers 也容易变成一种"流程崇拜"——遵守了流程就以为做对了，而忽略了真正重要的事情：**理解问题、设计解决方案、验证正确性**。流程是手段，不是目的。

---

## 十、结语

这个项目让我深刻体会到：当 LLM 能完成大部分编码工作时，工程师的真正价值在于：

1. **判断力**：知道做什么、不做什么
2. **设计力**：将模糊需求转化为清晰的 SPEC
3. **审查力**：发现 AI 生成代码中的问题
4. **工程力**：处理凭据、分发、部署等"脏活累活"

AI 可以写代码，但不能替你思考。Superpowers 给了你"流程脚手架"，但建筑的蓝图和质量，仍然取决于你。
